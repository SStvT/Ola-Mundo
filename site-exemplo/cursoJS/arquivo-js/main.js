/*
function bt() {
    var n1 = Number(window.prompt("Digite um número:"))
    var n2 = Number(window.prompt("Digite outro número:"))
    var n3 = n1+n2 // Soma // Conversão int = n3.parseInt(n) // float = n3.parse.Int(n3) // -----> Number(n)
    //window.alert("A soma dos números é igual a: " + n3) // Concatenação
    window.alert(`A soma dos números é igual a: ${n3}`) // n3.lenght conta quantos caracteres a string tem
    // n3.toUpperCase // leva os caracteres pra maiusculo // toLowerCase minusculo // 
    // n3.toFixed(Casas depois da vírgula)
    // n3.toFixed(Casas depois da vírgula).replace(',' , '.')
    // n3.toLocaleString('pt-br', {style: 'currency', currency: 'BRL'})
} */
//Operadors JS

function 

